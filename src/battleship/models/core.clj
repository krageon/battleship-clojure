;; Battleship in clojure core

;; Printing the board

(def bs-board-atom (atom {}))

(def bs-board-print
  (let [default "~"]
    (fn [] (
            loop
            )
      )))

(bs-board-print)

;; Picking ship locations


;; Randomised ship setup


;; Making a move

